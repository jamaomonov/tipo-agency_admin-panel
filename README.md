# Admin Panel - Админ-панель интернет-магазина

Современная админ-панель для управления интернет-магазином, построенная на React, TypeScript и Tailwind CSS.

## 🚀 Возможности

### 📊 Dashboard
- Статистика по категориям, товарам, вариантам и изображениям
- Обзор статусов товаров и остатков на складе
- Отображение последних изменений

### 🏷️ Управление категориями
- Создание, редактирование и удаление категорий
- Поиск и фильтрация
- Валидация данных

### 📦 Управление товарами
- Полный CRUD для товаров
- Расширенная информация о товарах
- Статусы: активный, новый, со скидкой
- Связь с категориями

### 🎨 Управление вариантами товаров
- Создание вариантов с характеристиками (цвет, размер, материал)
- Управление ценами и остатками
- Фильтрация по статусу остатков
- Поиск по всем характеристикам

### 🖼️ Управление изображениями
- Загрузка изображений для вариантов товаров
- Drag & Drop интерфейс
- Установка главного изображения
- Просмотр галереи изображений
- Фильтрация по товарам и вариантам

## 🛠️ Технологии

- **Frontend**: React 18, TypeScript, Vite
- **UI**: Tailwind CSS, Radix UI, Lucide Icons
- **Роутинг**: React Router DOM
- **HTTP клиент**: Fetch API с кастомной оберткой
- **Формы**: React Hook Form (готово к интеграции)
- **Валидация**: Zod (готово к интеграции)

## 📦 Установка и запуск

### Предварительные требования
- Node.js 18+ 
- npm или yarn

### Установка зависимостей
```bash
cd tipo-agency_admin-panel
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

### Просмотр собранного приложения
```bash
npm run preview
```

## 🔧 Конфигурация API

### Настройка базового URL
Отредактируйте файл `src/config/api.ts`:

```typescript
export const API_CONFIG = {
  base_url: 'http://localhost:8000', // Измените на ваш API сервер
  admin_prefix: '/admin',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
} as const;
```

### Структура API эндпоинтов
- `GET /api/v1/categories/` - список категорий
- `POST /api/v1/categories/` - создание категории
- `PUT /api/v1/categories/{id}` - обновление категории
- `DELETE /api/v1/categories/{id}` - удаление категории

- `GET /api/v1/products/` - список товаров
- `POST /api/v1/products/` - создание товара
- `PUT /api/v1/products/{id}` - обновление товара
- `DELETE /api/v1/products/{id}` - удаление товара

- `GET /api/v1/variants/` - список вариантов
- `POST /api/v1/variants/` - создание варианта
- `PUT /api/v1/variants/{id}` - обновление варианта
- `DELETE /api/v1/variants/{id}` - удаление варианта

- `GET /api/v1/images/` - список изображений
- `POST /api/v1/images/` - загрузка изображения
- `PUT /api/v1/images/{id}/set-main` - установка главного изображения
- `DELETE /api/v1/images/{id}` - удаление изображения

## 📁 Структура проекта

```
src/
├── components/          # UI компоненты
│   ├── ui/             # Базовые UI компоненты
│   ├── Layout.tsx      # Основной макет
│   └── ImageUpload.tsx # Компонент загрузки изображений
├── config/             # Конфигурация
│   └── api.ts          # Настройки API
├── data/               # Моковые данные
│   └── mockData.ts     # Данные для разработки
├── hooks/              # Кастомные хуки
│   ├── use-api.ts      # Хук для работы с API
│   └── use-toggle.ts   # Хук переключателя
├── lib/                # Утилиты
│   ├── api-client.ts   # HTTP клиент
│   └── utils.ts        # Общие утилиты
├── pages/              # Страницы приложения
│   ├── Dashboard.tsx   # Главная страница
│   ├── Categories.tsx  # Управление категориями
│   ├── Products.tsx    # Управление товарами
│   ├── Variants.tsx    # Управление вариантами
│   ├── Images.tsx      # Управление изображениями
│   ├── ProductDetails.tsx # Детали товара
│   └── VariantManager.tsx # Менеджер вариантов
├── services/           # API сервисы
│   ├── categories-api.ts
│   ├── products-api.ts
│   ├── variants-api.ts
│   ├── images-api.ts
│   └── index.ts
└── types/              # TypeScript типы
    └── index.ts
```

## 🎯 Основные функции

### Управление товарами
1. **Создание товара**: Заполните форму с названием, описанием, категорией и статусами
2. **Редактирование**: Измените любые поля товара
3. **Детальный просмотр**: Перейдите к полной информации о товаре с вариантами и изображениями
4. **Удаление**: Удалите товар со всеми связанными данными

### Управление вариантами
1. **Создание варианта**: Укажите цвет, размер, материал, цену и остаток
2. **Фильтрация**: Фильтруйте по товарам, статусу остатков
3. **Поиск**: Ищите по названию товара, характеристикам, цене
4. **Управление изображениями**: Загружайте и управляйте изображениями для каждого варианта

### Управление изображениями
1. **Загрузка**: Drag & Drop или выбор файлов
2. **Главное изображение**: Установите главное изображение для варианта
3. **Галерея**: Просматривайте все изображения товара
4. **Фильтрация**: Фильтруйте по товарам и вариантам

## 🔒 Безопасность

- Валидация данных на клиенте
- Обработка ошибок API
- Таймауты для запросов
- Безопасная загрузка файлов

## 🚀 Развертывание

### Netlify
Проект готов к развертыванию на Netlify. Файл `netlify.toml` уже настроен.

### Vercel
```bash
npm install -g vercel
vercel
```

### Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "run", "preview"]
```

## 🤝 Разработка

### Добавление новых функций
1. Создайте компонент в `src/components/`
2. Добавьте типы в `src/types/index.ts`
3. Создайте API сервис в `src/services/`
4. Добавьте страницу в `src/pages/`
5. Обновите роутинг в `src/App.tsx`

### Стилизация
- Используйте Tailwind CSS классы
- Следуйте дизайн-системе Radix UI
- Добавляйте кастомные стили в `src/index.css`

## 📝 Лицензия

MIT License

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера
2. Убедитесь, что API сервер запущен
3. Проверьте настройки в `src/config/api.ts`
4. Создайте issue в репозитории

---

**Версия**: 1.0.0  
**Последнее обновление**: Декабрь 2024
